# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: windows-latest


steps:
- task: SqlAzureDacpacDeployment@1
  inputs:
    azureSubscription: 'ADO-DB-SC'
    AuthenticationType: 'server'
    ServerName: 'mhk-db-server.database.windows.net'
    DatabaseName: 'mhk-db'
    SqlUsername: 'testuser'
    SqlPassword: 'Password1234!'
    deployType: 'SqlTask'
    SqlFile: 'https://github.com/HussaanKhalid/sql-queries/blob/main/sql-test-query.sql'
    IpDetectionMethod: 'AutoDetect'

